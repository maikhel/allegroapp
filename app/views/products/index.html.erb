<h1>Products</h1>
	

<br>
<div class="table-responsive">
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Name </th>
        <th>Price</th>
        <th>Ending time </th>
        <th>Image</th>
        
      </tr>
    </thead>
    <tbody>
    
      <% @data.each do |item| %>
      <tr>
  	        <td><%= item.name %>
            <a href="<%= item.link %>" class="btn btn-link ">More</a><br>
            <button type="button" class="btn btn-info">Save to database</button>
            </td>
  	        <td><%= item.price %> zł</td>
  	        <td><%= item.ending_time.to_s[0..15] %></td>
  	        <td><img src="<%= item.thumb_url %>" class="img-thumbnail"></td>
  	   </tr>
        
      <% end %>
    </tbody>
  </table>
</div>
